---
- name: user module demo
  hosts: centos
  become: true
  vars:
    myuser: "test1"
  tasks:
    - name: enable user
      ansible.builtin.user:
        name: "{{ myuser }}"
        state: present
        password_lock: false
        shell: "/bin/bash"